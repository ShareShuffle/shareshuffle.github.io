# READ FIRST

Before editing this repository, read every Markdown file in `.project-context/summaries/`. Verify actual current files and Firebase targets. Never rebuild from an older patch. Preserve original artwork and do not generate replacement art unless explicitly requested.
