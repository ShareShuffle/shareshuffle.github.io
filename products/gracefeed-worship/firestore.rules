rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access passes through the authenticated, allowlisted server API.
    match /{document=**} { allow read, write: if false; }
  }
}
